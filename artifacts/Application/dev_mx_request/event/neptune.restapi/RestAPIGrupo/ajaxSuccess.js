const grupo = modelModelArrayGrupo.getData();
// modelSelectGrupo.setData(grupo);
modelComboBoxGrupo.setData(grupo);

// InputGpo.setModel( modelModelArrayGrupo );

// let itemSugg = new sap.m.SuggestionItem("itemSugg", { key: "{lst_nombre}", text: "{lst_nombre}" }); 
// InputGpo.bindAggregation("suggestionItems", "/", itemSugg);